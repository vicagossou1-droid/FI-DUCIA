SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional: shared secret (>= 8 chars after trim) so local batches of 10 GPS rows are
# encrypted (AES-256-CBC) before upload. Not from Supabase — generate e.g. with:
#   openssl rand -base64 32
# Put the output on ONE line below. Do not commit real keys to git.
SYNC_ENCRYPTION_KEY=
